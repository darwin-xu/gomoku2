{
  "name": "gomoku2",
  "version": "1.0.0",
  "description": "A modern Gomoku (Five in a Row) game with AI support",
  "main": "dist/server/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server/server.js",
    "dev": "ts-node src/server/server.ts",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "train": "ts-node src/scripts/train.ts",
    "play": "ts-node src/scripts/play.ts"
  },
  "keywords": ["gomoku", "game", "ai", "typescript"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
